import Foundation
import SwiftUI

// MARK: - Module entry point
// This module provides a Server-Driven UI rendering engine that translates
// a JSON page description into native SwiftUI (iOS) or Compose (Android via Skip).

public struct SDUIRenderSwift {
    private init() {}
}
